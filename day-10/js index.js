function Sum(a,b) {
    sum=a+b;
    console.log(sum;)
}
sum(4,6);

const counter= document.getElementById("counter");

let count = 0;
function incrementValue() {
    count--;
    Counter.innerText = count;
}
function decValue() {
    count--;
    Counter.innerText = count;
}